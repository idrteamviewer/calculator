# Sample Calculator

A small command-line calculator written in Python. It supports addition,
subtraction, multiplication, and division.

## Run it

```text
python welcome.py
```

Enter expressions with spaces around the operator, for example:

```text
> 12.5 * 4
= 50
```

Type `q` to quit.

## Run the tests

```text
python -m unittest check.py
```